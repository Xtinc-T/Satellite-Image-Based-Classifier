# Core
numpy>=1.24
pillow>=10.0
matplotlib>=3.7
tqdm>=4.66

# CV
opencv-python>=4.8

# PyTorch (CPU/MPS/CUDA wheels vary by platform; install normally with pip)
torch>=2.1
torchvision>=0.16

# Notebook
jupyter>=1.0
nbconvert>=7.0

# Optional (only if you keep the UI cells)
# gradio>=4.0
